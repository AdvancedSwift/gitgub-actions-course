name: 03 - Workflow Runner
on: push
jobs:
  ubuntu-echo:
      runs-on: ubuntu-latest
      steps:
        - name: Show OS
          run: |
            echo "This is running on ubuntu"
            echo "runner os $RUNNER_OS"
            
